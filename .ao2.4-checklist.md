# AO2.4 closure checklist

- [x] Expose `just benchmark --target tcp` and `--target tcp-tls` as the only
  public peer-wire benchmark selectors; map each to the ordinary daemon launch
  flag without a Cargo feature or alternate transport implementation.
- [x] Record and validate reproducible public provenance: candidate revision,
  binary version, peer-wire mode, OS/architecture, hook mode, frames per
  connection, samples, and sanitized command/baseline identity.
- [x] Make comparison/report identity mode-aware so plaintext compares only to
  compatible plaintext evidence and mTLS has its own later same-mode baseline.
- [x] Cover selector parsing, daemon launch arguments, schema/report rendering,
  and safe redaction with focused tests.
- [x] Update the canonical benchmark/smoke procedures and AO evidence ledger;
  record unavailable physical hosts as bounded blocked evidence, never PASS.
- [x] Re-read the plan; targeted tests, `just benchmark-report --rebuild`,
  `just reports-index --check`, `just lint`, and `just test` pass. Both public
  target commands were invoked against the shipped release pair. The M4 `tcp`
  campaign is a distinct blocked evidence result: it retains the stable
  `missing_compatible_plaintext_baseline` failure code rather than claiming a
  regression pass; M4 `tcp-tls` also records the ambient-daemon isolation
  block. M5 and FastPC4 remain separately blocked rather than PASS.
