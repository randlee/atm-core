[package]
name = "atm-daemon-client"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
description = "Shared same-host ATM daemon bootstrap helpers for thin clients."
repository.workspace = true
homepage.workspace = true

[dependencies]
atm-core = { package = "agent-team-mail-core", path = "../atm-core", version = "1.3.3" }
atm-storage = { path = "../atm-storage", version = "1.3.3" }
bytes = "1"
fs2 = "0.4"
interprocess = "2.4.2"
serde = { workspace = true }
serde_json = { workspace = true }
tracing = "0.1"

[dev-dependencies]
atm-core = { package = "agent-team-mail-core", path = "../atm-core", version = "1.3.3" }
tempfile = "3"
