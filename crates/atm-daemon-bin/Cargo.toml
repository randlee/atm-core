[package]
name = "atm-daemon-bin"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
description = "ATM daemon binary wrapper with retained observability ownership."
repository.workspace = true
homepage.workspace = true

[[bin]]
name = "atm-daemon"
path = "src/main.rs"

[dependencies]
atm-core = { package = "agent-team-mail-core", path = "../atm-core", version = "1.2.0" }
atm-daemon = { path = "../atm-daemon", version = "1.2.0" }
sc-observability.workspace = true
sc-observability-types.workspace = true
serde_json.workspace = true
tracing.workspace = true

[dev-dependencies]
tempfile = "3"
serial_test = "3"
