[package]
name = "atm-runtime"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
description = "ATM concrete runtime composition crate."
repository.workspace = true
homepage.workspace = true

[lib]
name = "atm_runtime"

[features]
test-utils = []

[dependencies]
atm-core = { package = "agent-team-mail-core", path = "../atm-core", version = "1.4.0-beta.36" }
atm-storage = { path = "../atm-storage", version = "1.4.0-beta.36" }
