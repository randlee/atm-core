boundary_id = "BOUNDARY-PeerResendScheduler-Daemon"
owner_package = "atm-daemon"
owner_crate_path = "atm_daemon"
name = "PeerResendScheduler"

[public]
trait = "none"

[implementation]
type = "PeerResendScheduler"
module = "atm_daemon::runtime_health::peer_resend_scheduler"
visibility = "pub(crate)"
constructor = "pub(crate)"

[composition]
roots = []

[ownership]
io_owns = []
io_forbidden = ["dns_thread", "outbound_worker", "tls", "nudge_emission", "graft_delivery", "process_spawn"]

[dependencies]
allowed_dependents = []
allowed_dependencies = []
forbidden_edges = ["atm -> atm-daemon", "atm-graft -> atm-daemon"]

[references]
scope = "outside_owner_crate"
forbidden = ["PeerResendScheduler"]

[contracts]
request_types = []
response_types = []
error_types = []

[testing]
allowed_test_double_paths = []
forbidden_test_bypasses = []

[enforcement]
lint_rules = ["LINT-BOUNDARY-PEER-RESEND-SCHEDULER-REMOVED"]
review_gates = ["no_runtime_wiring", "no_background_worker", "compile_failing_deprecation"]

[status]
state = "retired"
notes = ["Removed: no production path may construct, schedule, queue, or consult PeerResendScheduler. The retained type is deprecated and daemon compilation denies deprecated uses."]
