boundary_id = "BOUNDARY-NonClaudeOutbound-Daemon"
owner_package = "atm-daemon"
owner_crate_path = "atm_daemon"
name = "DaemonNonClaudeOutboundAdapter"

[public]
trait = "NonClaudeOutbound"

[implementation]
type = "DaemonNonClaudeOutbound"
module = "atm_daemon::non_claude_outbound_runtime"
visibility = "pub(crate)"
constructor = "pub(crate)"

[composition]
roots = []

[ownership]
io_owns = ["outbound_non_claude_payload_delivery"]
io_forbidden = ["sqlite", "compatibility_jsonl_append"]

[dependencies]
allowed_dependents = []
allowed_dependencies = ["atm-core"]
forbidden_edges = ["atm -> atm-daemon", "atm-graft -> atm-daemon"]

[references]
scope = "outside_owner_crate"
forbidden = ["DaemonNonClaudeOutbound", "std::process::Command"]

[contracts]
request_types = ["logical message delivery requests"]
response_types = ["typed non-Claude delivery results"]
error_types = ["AtmError"]
notes = ["The daemon-owned adapter is wired into the production retained runtime via atm_daemon::runtime_health::daemon_local_runtime(), which installs Arc::new(DaemonNonClaudeOutbound::new()) in the shared delivery-boundary constructor."]

[testing]
allowed_test_double_paths = []
forbidden_test_bypasses = []

[enforcement]
lint_rules = ["LINT-BOUNDARY-NON-CLAUDE-OUTBOUND-DAEMON-REFERENCES"]
review_gates = ["no_public_impl"]

[status]
state = "retired"
notes = ["Historical legacy-daemon record. AL.8 selects LocalFileNonClaudeOutbound through the replacement bootstrap's backend-neutral RuntimeAssembly; this legacy adapter is reference-only until Phase AM deletes it."]
