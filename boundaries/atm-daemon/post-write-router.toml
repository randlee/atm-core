boundary_id = "BOUNDARY-PostWriteRouter-Daemon"
owner_package = "atm-daemon"
owner_crate_path = "atm_daemon"
name = "DaemonPostWriteRouter"

[public]
trait = "PostWriteRouter"

[implementation]
type = "DaemonRequestDispatcher"
module = "atm_daemon::runtime_health::peer_delivery_router"
visibility = "pub(crate)"
constructor = "pub(crate)"

[composition]
roots = []

[ownership]
io_owns = ["post_persistence_route_classification", "received_hook_orchestration"]
io_forbidden = ["sqlite", "dns", "socket_io", "tls", "hook_execution", "graft_delivery", "retry_queue"]

[dependencies]
allowed_dependents = []
allowed_dependencies = ["atm-core"]
forbidden_edges = ["atm -> atm-daemon", "atm-graft -> atm-daemon"]

[references]
scope = "outside_owner_crate"
forbidden = []

[contracts]
request_types = ["newly persisted MessageRecord", "RequestDeadline"]
response_types = ["retained WarningEntry diagnostics", "legacy PostCommitWorkKey signal"]
error_types = ["none; receiver-hook failures become warnings"]

[testing]
allowed_test_double_paths = []
forbidden_test_bypasses = []

[enforcement]
lint_rules = ["LINT-BOUNDARY-POST-WRITE-ROUTER"]
review_gates = ["new_persistence_only", "single_received_hook_call_site", "no_direct_peer_transport", "no_hook_queue"]

[status]
state = "retired"
notes = ["Historical legacy-daemon record. AM.3 deleted RuntimeComposition; StorageAndNudgeRouter invokes the sealed received-hook selector after a newly durable write."]
