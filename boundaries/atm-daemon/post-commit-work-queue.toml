boundary_id = "BOUNDARY-PostCommitWorkQueue-Daemon"
owner_package = "atm-daemon"
owner_crate_path = "atm_daemon"
name = "PostCommitWorkQueue"

[public]
trait = "PostCommitWorkQueue"

[implementation]
type = "PeerPostCommitWorkQueue"
module = "atm_daemon::runtime_health::post_commit_work"
visibility = "pub(crate)"
constructor = "pub(crate)"

[composition]
roots = []

[ownership]
io_owns = ["legacy_peer_delivery_wakeup_only"]
io_forbidden = ["sqlite", "receipt", "hook_execution", "process_spawn", "retry_queue"]

[dependencies]
allowed_dependents = []
allowed_dependencies = ["atm-core"]
forbidden_edges = ["atm -> atm-daemon", "atm-graft -> atm-daemon"]

[references]
scope = "outside_owner_crate"
forbidden = ["PeerPostCommitWorkQueue"]

[contracts]
request_types = ["PostCommitWorkKey"]
response_types = ["infallible scheduling signal"]
error_types = ["none"]

[testing]
allowed_test_double_paths = []
forbidden_test_bypasses = []

[enforcement]
lint_rules = ["LINT-BOUNDARY-POST-COMMIT-WORK-QUEUE"]
review_gates = ["identifier_only", "no_admission_wait", "no_direct_sqlite", "no_received_hook"]

[status]
state = "retired"
notes = ["Historical legacy-daemon record. AM.3 deleted RuntimeComposition; the active runtime does not compose peer wake-up, resend, or replay work."]
