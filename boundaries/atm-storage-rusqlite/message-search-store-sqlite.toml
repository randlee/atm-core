boundary_id = "BOUNDARY-SqliteMessageSearchStore"
owner_package = "atm-storage-rusqlite"
owner_crate_path = "atm_storage_rusqlite"
name = "SqliteMessageSearchStore"

[public]
trait = "MessageSearchStore"

[implementation]
type = "SqliteMessageSearchStore"
module = "atm_storage_rusqlite::search_store"
visibility = "private"
constructor = "private"

[composition]
roots = ["atm_storage_rusqlite::SqliteStorageBackend::new"]

[ownership]
io_owns = ["fts5_compilation", "json1_filter_compilation", "search_reader_lane", "search_projection_rebuild"]
io_forbidden = ["http_runtime", "template_rendering", "socket_io", "process_spawn"]

[dependencies]
allowed_dependents = ["atm-daemon-bootstrap", "atm-runtime-test-support"]
allowed_dependencies = ["atm-storage", "rusqlite", "tokio"]
forbidden_edges = ["atm -> atm-storage-rusqlite", "atm-daemon -> atm-storage-rusqlite", "atm-graft -> atm-storage-rusqlite", "atm-runtime -> atm-storage-rusqlite", "atm-storage-rusqlite -> atm-runtime"]

[references]
scope = "inside_owner_crate"
forbidden = ["atm_http_runtime", "TemplateComposer"]

[contracts]
request_types = ["MessageSearchQuery", "SearchDeadline"]
response_types = ["MessageSearchPage"]
error_types = ["AtmError"]

[testing]
allowed_test_double_paths = []
forbidden_test_bypasses = ["direct_http_sqlite_reader"]

[enforcement]
lint_rules = ["LINT-BOUNDARY-MESSAGE-SEARCH-STORE"]
review_gates = ["private_sqlite_impl", "no_http_sqlite_reader"]

[status]
state = "concrete_landed"
