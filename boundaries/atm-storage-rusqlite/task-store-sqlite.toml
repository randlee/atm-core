boundary_id = "BOUNDARY-TaskStore-Sqlite"
owner_package = "atm-storage-rusqlite"
owner_crate_path = "atm_storage_rusqlite"
name = "SqliteTaskStore"

[public]
trait = "TaskStore"

[implementation]
type = "SqliteTaskStore"
module = "atm_storage_rusqlite::task_store"
visibility = "private"
constructor = "pub(crate)"

[composition]
roots = ["atm_storage_rusqlite::SqliteStorageBackend::new"]

[ownership]
io_owns = ["sqlite", "task_event_audit_append", "task_state_transition_in_writer_transaction"]
io_forbidden = ["message_delivery", "process_spawn"]

[dependencies]
allowed_dependents = ["atm-daemon-bootstrap", "atm-runtime-test-support"]
allowed_dependencies = ["atm-storage", "rusqlite"]
forbidden_edges = ["atm-storage-rusqlite -> atm-core", "atm-storage-rusqlite -> atm-runtime", "atm-daemon -> atm-storage-rusqlite"]

[references]
scope = "outside_owner_crate"
forbidden = ["SqliteTaskStore", "rusqlite::Connection"]

[contracts]
request_types = ["MemberKey", "TaskId", "IsoTimestamp", "ReminderOutcome", "MessageWriteOrigin", "EscalationScope"]
response_types = ["Option<TaskRow>", "Vec<TaskRow>", "Vec<TaskEventRow>", "Vec<String>", "bool"]
error_types = ["AtmError"]
notes = [
  "retained v1 compatibility writer behavior remains during coexistence",
  "writer/ops.rs routes canonical v2 transitions only through AsyncTaskMutationStore",
  "D8 escalation recipient methods persist daemon/team scope rows in SQLite",
  "review-only governed source module: crates/atm-storage-rusqlite/src/writer/task_legacy_ops.rs; lint_rules remains undispatched while [enforcement].io_forbidden_source_modules scanning is real and active (added in prior fix-4 round)",
]

[testing]
allowed_test_double_paths = []
forbidden_test_bypasses = ["rusqlite::Connection"]

[enforcement]
lint_rules = ["LINT-BOUNDARY-TASK-STORE-SQLITE"]
review_gates = ["manual_architecture_review"]

[enforcement.io_forbidden_source_modules]
message_delivery = ["atm_storage_rusqlite::writer::task_ops", "atm_storage_rusqlite::writer::task_legacy_ops"]
process_spawn = ["atm_storage_rusqlite::writer::task_ops", "atm_storage_rusqlite::writer::task_legacy_ops"]

[status]
state = "concrete_landed"
notes = ["AX.3"]
