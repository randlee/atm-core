boundary_id = "BOUNDARY-MailStore"
owner_package = "atm-core"
owner_crate_path = "atm_core"
name = "MailStore"

[public]
trait = "MailStore"
notes = "trait landed in atm_core::boundary; concrete implementation now lives in crates/atm-rusqlite/src/lib.rs as SqliteMailStore"

[implementation]
visibility = "trait_only"
constructor = "none"

[composition]
roots = []

[ownership]
io_owns = ["message_lifecycle_state", "mailbox_projection_state"]
io_forbidden = ["socket_io", "process_spawn"]

[dependencies]
allowed_dependents = ["atm-rusqlite", "atm-daemon", "atm-runtime-test-support"]
allowed_dependencies = []
forbidden_edges = ["atm-core -> atm-daemon", "atm-core -> atm-rusqlite"]

[references]
scope = "outside_owner_crate"
forbidden = []

[contracts]
request_types = ["MailStore method inputs"]
response_types = ["atm-core mailbox DTOs"]
error_types = ["AtmError"]

[testing]
allowed_test_double_paths = []
forbidden_test_bypasses = ["rusqlite::Connection"]

[enforcement]
lint_rules = ["LINT-BOUNDARY-MAILSTORE-REFERENCES"]
review_gates = ["no_concrete_store_leakage"]

[status]
state = "concrete_landed"
notes = ["trait plus request/response structs landed in atm_core::boundary", "concrete store implementation landed in crates/atm-rusqlite/src/lib.rs as SqliteMailStore", "mail state remains distinct from task and roster state"]
