boundary_id = "BOUNDARY-WatchEventSource"
owner_package = "atm-core"
owner_crate_path = "atm_core"
name = "WatchEventSource"

[public]
trait = "WatchEventSource"

[implementation]
visibility = "trait_only"
constructor = "none"

[composition]
roots = []

[ownership]
io_owns = ["filesystem_watch_events", "watch_event_delivery"]
io_forbidden = ["sqlite", "process_spawn"]

[dependencies]
allowed_dependents = []
allowed_dependencies = []
forbidden_edges = ["atm-core -> atm-daemon", "atm-core -> atm-storage-rusqlite"]

[references]
scope = "outside_owner_crate"
forbidden = ["notify::recommended_watcher"]

[contracts]
request_types = ["watch subscription requests"]
response_types = ["watch event batches"]
error_types = ["AtmError"]

[testing]
allowed_test_double_paths = []
forbidden_test_bypasses = ["notify::recommended_watcher"]

[enforcement]
lint_rules = ["LINT-BOUNDARY-WATCH-EVENT-SOURCE-REFERENCES"]
review_gates = ["no_watch_io_outside_boundary"]

[status]
state = "retired"
notes = ["Phase AD removed the accepted atm_core boundary trait and daemon construction path for watch polling.", "Named watch DTOs remain historical-only protocol scaffolding until a later cleanup sprint deletes them."]
