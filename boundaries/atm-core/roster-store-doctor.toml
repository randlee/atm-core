boundary_id = "BOUNDARY-RosterStoreDoctor"
owner_package = "atm-core"
owner_crate_path = "atm_core"
name = "RosterStoreDoctor"

[public]
trait = "RosterStoreDoctor"
notes = "paired subsystem-owned diagnostics boundary for the RosterStore capability"

[implementation]
visibility = "trait_only"
constructor = "none"

[composition]
roots = []

[ownership]
io_owns = ["roster_store_diagnostics"]
io_forbidden = ["socket_io", "process_spawn"]

[dependencies]
allowed_dependents = ["atm-storage-rusqlite", "atm-daemon"]
allowed_dependencies = []
forbidden_edges = ["atm-core -> atm-daemon", "atm-core -> atm-storage-rusqlite"]

[references]
scope = "outside_owner_crate"
forbidden = []

[contracts]
request_types = ["RosterStoreDoctor method inputs"]
response_types = ["RosterStoreDoctorReport", "DoctorFinding"]
error_types = ["AtmError"]

[testing]
allowed_test_double_paths = []
forbidden_test_bypasses = ["rusqlite::Connection"]

[enforcement]
lint_rules = ["LINT-BOUNDARY-ROSTERSTOREDOCTOR-REFERENCES"]
review_gates = ["no_concrete_store_leakage"]

[status]
state = "concrete_landed"
notes = ["shared doctor-trait contract landed in atm_core::boundary", "roster-store diagnostic implementation is wired through the atm-storage-rusqlite doctor adapter"]
