# AO2.15 Worktree Checklist — Official Benchmark Trigger

Source of truth: `docs/plans/phase-ao2/sprint-AO2-15-benchmark-official-trigger.md`.
Each item needs focused coverage, local validation, and a pushed commit before
the next item is complete.

- [ ] D1. Verify the existing `just benchmark-publish` contract with fixtures:
  only report artifacts stage, and a stale index prevents every staging action.
- [ ] D2. Add the non-interactive `benchmark_official.py` orchestrator and
  `just benchmark-official`: account/doctor/git preflight, fresh build/sign,
  direct matrix execution, independent rendering, safe publication and
  deploy-key push, plus the 0/1/2 verdict contract.
- [ ] D2 tests. Cover wrong account, unhealthy doctor, dirty tree, stranded
  push success/rejection, missing binaries, PASS/FAIL precedence across
  render/publish/push, and no-tty behavior.
- [ ] D3. Add the SSH/launchd trigger template and benchmark-run skill
  runbook, including explicit deploy-key and host-key trust installation steps.
- [ ] Validation. Run focused tests, `just lint`, and `just test`; commit and
  push the complete AO2.15 implementation, then send the exact SHA to
  team-lead.
