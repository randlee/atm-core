# AO2.14 Worktree Checklist — Daemon-Owned Peer Connection Pooling

Branch point: `a315dfb480878d8d6ff155f29200fdb694fb1f6f`

Source of truth: `docs/plans/phase-ao2/sprint-AO2-14-peer-connection-pooling.md`.
Each checked item must have focused tests, the applicable verification command,
and a pushed commit before the next item starts.

- [x] 1. Add the TLS-opaque, daemon-owned `PeerConnectionPool`: bounded
  authority-keyed post-handshake HTTP/1 senders, connection-driver ownership,
  health-aware RAII borrowing, idle eviction, bounded teardown, and the
  reservation-counter invariant.
- [x] 2. Reuse one daemon-lifetime plaintext `reqwest::Client`; update
  `DirectPeerTcpConnector` and router builder wiring without creating a custom
  plaintext pool.
- [x] 3. Integrate the pool into `StorageAndNudgeRouter`: acquire, exactly one
  exchange, healthy release, failed discard, and acquire-time-only redial.
- [x] 4. Wire the shared client and optional mTLS pool in
  `atm-daemon-bootstrap`'s replacement composition root.
- [x] 5. Add validated `PeerPoolConfig` defaults and CLI/environment plumbing:
  max-per-peer=4, max-pooled-total=32, idle-timeout=60s.
- [x] 6. Preserve the existing `HttpRuntimeClientFailure` taxonomy and prove
  existing router dispatch behavior stays unchanged.
- [x] 7. Author the next-numbered ADR for redial and delivery-attempt safety;
  add it to the ADR index and provide the required failure-mode analysis.
- [x] 8. Review and make only explicit, necessary boundary-guard updates.
- [x] 9. Run all AO2.14 acceptance tests: reuse, peer isolation, eviction,
  reconnect/no post-exchange retry, bounds/overflow, config parsing, counter
  lifetime, driver-task completion, teardown, ADR gate, and unchanged dispatch
  tests. Evidence: `cargo test -p atm-http-runtime` (119 passed),
  `cargo test -p atm-daemon-bootstrap --lib` (17 passed), and
  `cargo test -p atm-architecture --test boundary_enforcement` (63 passed);
  targeted clippy and formatting checks passed.
- [ ] 10. Run full validation (`just test`, lint/clippy as applicable), then the
  required M5 before/after benchmark and real mTLS dispatch burst; publish only
  through the approved reporting pipeline and do not change baselines.
  Local validation is complete (`just test`: 662 passed, 9 skipped). Matched M5
  full f8 campaigns completed with byte-identical snapshot restoration:
  parent `a315dfb48` = sqlite 8029.59 / UDS 13136.35 / TCP 12360.03 /
  TCP+TLS 10448.23 msg/s; candidate `971b92fcb` = sqlite 9378.42 / UDS
  14777.52 / TCP 13378.15 / TCP+TLS 11032.06 msg/s. Both are below the
  current reviewed floors, therefore the gate remains open. The matrix is a
  local admission benchmark and never executes the outbound
  `StorageAndNudgeRouter` peer-dispatch path, so it cannot establish the
  planned pooling-performance claim. The required real cross-host mTLS burst
  also remains open: there is no dedicated benchmark account on M4, and using
  the randlee primary database as a fixture is prohibited. Interim signal:
  `selected_router_reuses_one_opaque_peer_connection_across_three_loopback_writes`
  starts a second authenticated loopback runtime, dispatches three canonical
  host-qualified writes through `StorageAndNudgeRouter`, and proves one opaque
  outbound establishment plus three durable remote records. It is deliberately
  not presented as a physical mTLS or cross-host acceptance result.
