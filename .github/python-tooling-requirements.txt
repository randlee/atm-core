# Canonical Python toolchain for local validation and GitHub Actions.
#
# Keep every workflow and developer bootstrap on this one file.  Exact pins
# prevent a hosted runner's current Python image from silently choosing a
# different release toolchain than another workflow or local environment.
build==1.3.0
codespell==2.4.3
maturin==1.11.5
pydantic==2.12.5
twine==6.1.0
