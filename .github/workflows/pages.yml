name: GitHub Pages

on:
  push:
    branches:
      - integrate/phase-ai-31-33
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install just
        uses: taiki-e/install-action@just

      - name: Set up Node.js for Mermaid rendering
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Cache Puppeteer Chromium
        uses: actions/cache@v4
        with:
          path: ~/.cache/puppeteer
          key: ${{ runner.os }}-puppeteer-chrome-v1

      - name: Provision Puppeteer Chromium
        run: npx -y puppeteer browsers install chrome

      - name: Verify generated procedure pages
        run: just procedures --check

      - name: Verify generated report index
        run: just reports-index --check

      - name: Upload the site artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
