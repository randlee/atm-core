# AG.1 restores the repo-wide boundary-visibility regex to treat any
# `pub(...)` qualifier as public, then narrows the one sanctioned exception set
# back down to the documented PeerServerTransport contract.

[[allow]]
rule = "SCB-BOUNDARY-VISIBILITY-001"
path = "crates/atm-daemon/src/peer_transport/server.rs"
symbol = "PeerServerTransport"
why = "ADR-028 and sprint AG.1 require the concrete peer listener type to stay crate-private via pub(super) inside atm_daemon::peer_transport"
sunset_sprint = "AG.1"

[[allow]]
rule = "SCB-BOUNDARY-VISIBILITY-003"
path = "crates/atm-daemon/src/peer_transport/server.rs"
symbol = "new"
why = "ADR-028 and sprint AG.1 require PeerServerTransport::new to stay crate-private via pub(super) inside atm_daemon::peer_transport"
sunset_sprint = "AG.1"

[[allow]]
rule = "SCB-BOUNDARY-VISIBILITY-003"
path = "crates/atm-daemon/src/peer_transport/server.rs"
symbol = "start"
why = "ADR-028 and sprint AG.1 require PeerServerTransport::start to stay crate-private via pub(super) inside atm_daemon::peer_transport"
sunset_sprint = "AG.1"

[[allow]]
rule = "SCB-BOUNDARY-VISIBILITY-003"
path = "crates/atm-daemon/src/peer_transport/server.rs"
symbol = "shutdown"
why = "ADR-028 and sprint AG.1 require PeerServerTransport::shutdown to stay crate-private via pub(super) inside atm_daemon::peer_transport"
sunset_sprint = "AG.1"

[[allow]]
rule = "SCB-BOUNDARY-VISIBILITY-003"
path = "crates/atm-daemon/src/peer_transport/server.rs"
symbol = "reload"
why = "ADR-028 and sprint AG.1 require PeerServerTransport::reload to stay crate-private via pub(super) inside atm_daemon::peer_transport"
sunset_sprint = "AG.1"

[[allow]]
rule = "SCB-BOUNDARY-VISIBILITY-003"
path = "crates/atm-daemon/src/peer_transport/server.rs"
symbol = "bound_addr"
why = "ADR-028 and sprint AG.1 require PeerServerTransport::bound_addr to stay crate-private via pub(super) inside atm_daemon::peer_transport while the runtime wrapper projects the observable listener state."
sunset_sprint = "AG.1"

[[allow]]
rule = "SCB-BOUNDARY-VISIBILITY-003"
path = "crates/atm-daemon/src/peer_transport/server.rs"
symbol = "bound_addr_for_test"
why = "test-only peer listener helper stays crate-private via pub(super) inside atm_daemon::peer_transport"
sunset_sprint = "AG.1"
