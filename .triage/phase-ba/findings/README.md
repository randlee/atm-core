# Phase BA findings

Empty by design as of 2026-09-12: QA findings for BA.2–BA.5 exist only as
GitHub PR comments so far, not as canonical Turtle records. Add
`<finding-id>.ttl` records here (see `.triage/README.md` and
`docs/triage/ttl-repair.md`) if/when live B/I/M occurrence tracking is
needed for this phase; this placeholder exists only so the directory is
tracked and the findings validator (`validate-findings.py`) has a real,
non-erroring `--findings-dir` to read.
