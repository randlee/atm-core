# AO2.10 M5 v4 campaign evidence note

Campaign `20260824T030844Z-rand-m5` deliberately uses the seeded
`host_label=rand-m5`.  AO2.10's reviewed baseline set contains macOS target
floors only for that label, so the runner can fail closed before measurement
for an accidental `local` or otherwise unreviewed host label.

The label identifies the reviewed M5 measurement class; it is not inferred
from DNS, the account name, or a mutable machine property.  The four result
artifacts record baseline revision `1` and their immutable floor snapshot.
Their source revision is `4f0a734b6e534d5fc4682ac61d17e7d5f36abab6`.

This note applies only to this committed campaign.  Later baseline revisions
must not reinterpret its stored status or floor snapshot.
