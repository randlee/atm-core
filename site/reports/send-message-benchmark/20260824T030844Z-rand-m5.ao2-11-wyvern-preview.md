# AO2.11 Wyvern preview evidence

Command: `just benchmark-show`

The command rebuilt the report site, copied the newest campaign panel to
`artifacts/benchmark/preview/latest.html`, and opened it with Wyvern on macOS.
The operator visually confirmed that the Wyvern view rendered correctly on
2026-08-24. The displayed campaign values remain the immutable values from
source revision `4f0a734b6e534d5fc4682ac61d17e7d5f36abab6`; this preview does
not claim that those older measurements meet the current parity baseline.
