# AO2.11 Wyvern render transcript — 2026-08-26

This retained output closes the evidence gap in the original prose-only
preview note. It was captured against the newest committed benchmark panel;
no benchmark was run and no measurement values were changed.

```text
$ WYVERN_VIEWER=none just benchmark-show
env PATH="$PWD/.bootstrap-venv/bin:$PATH" "$PWD/.bootstrap-venv/bin/python" scripts/smoke/benchmark_report.py --rebuild
env PATH="$PWD/.bootstrap-venv/bin:$PATH" "$PWD/.bootstrap-venv/bin/python" scripts/smoke/benchmark_show.py
WYVERN_DIALOG_URL=http://127.0.0.1:63123/wizard/latest.html
exit status: 0
```

`WYVERN_VIEWER=none` suppresses only the local GUI while retaining Wyvern's
actual render invocation and emitted preview URL, making this evidence usable
in the headless verification environment as well as on macOS.
