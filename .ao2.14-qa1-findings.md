# AO2.14 PR #1020 — QA-1 Fix Ledger

Source: `FIX-AO214-1020-R1`; authoritative sprint doc:
`docs/plans/phase-ao2/sprint-AO2-14-peer-connection-pooling.md`.

- [x] RBQA-AO214-F003 — Renumber the new peer-write ADR to ADR-057, update
  index and source citation; prove ADR index has no duplicate number.
- [x] RUST-QA-AO214-001 — Rename retired `SelectedPeerTransport` /
  `peer_transport` family; update the stale architecture-test count in the
  AO2.14 checklist.
- [x] ARCH-QA-AO214-RULE003 — Not reproducible: `.just/check_line_counts.py`
  measures `client.rs` at 886 and `lib.rs` at 864 production lines, both
  below the enforced 1000-line ceiling; no stable code was split.
- [x] RBQA-AO214-F001 — Remove the obsolete unpooled `PeerStreamConnector`
  client path and corresponding router branch.
- [x] RSH-AO214-001 — The cited `ready()` call was already wrapped in
  `tokio::time::timeout(remaining, ...)`; add expired-budget coverage proving
  the idle sender is not redialed and its reservation is released.
- [x] RBQA-AO214-F002 — Close the visibility inconsistency through the same
  obsolete-client-path removal.
- [x] RBQA-AO214-F004 — Add a lightweight duplicate-ADR-number gate with
  direct test coverage.

Per-finding verification is recorded in the commits and final validation
message. Physical M4 mTLS proof and below-floor M5 benchmark results remain
explicitly out of scope.

## QA-2 follow-up

- [x] ATM-QA-101 — Add dedicated bounded driver-completion coverage for
  Overflow Drop after a failed exchange and Overflow Drop with no exchange.
