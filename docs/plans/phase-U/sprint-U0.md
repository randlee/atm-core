# Sprint U.0 — Remove Old `atm-graft` Implementation Line

```yaml
plan_type: sprint_plan
phase: U
sprint: "U.0"
status: completed
owner: team-lead
```

## Goal

Remove the old `atm-graft` implementation line before the restacked graft work
continues under the Phase U ownership rules.

## Completed Outcome

- the old graft implementation line was removed by `team-lead`
- later graft sprints in Phase U (`U.8` through `U.10`) assume that removal is
  complete and only restack the design around shared `atm-core` interfaces and
  a generic daemon surface
