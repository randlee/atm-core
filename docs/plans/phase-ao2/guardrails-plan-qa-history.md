# Guardrails Plan (AO2.15–AO2.17) — QA History

| Round | Reviewer | Commit | Result | Disposition |
|-------|----------|--------|--------|-------------|
| 1 | plan-scope-reviewer (sonnet) | `169e8e0ba` | FAIL — 3 Blocking (7-deliverable sprint spanning 5 closure types must split; D3/D4 split left as a runtime "team-lead's call"; D7 runbook had no AC), 4 Important (AC↔deliverable mapping unclear; manifest TOML, contract-hash schema, and criterion baseline all lacked code samples), 1 minor | Fixed in round-1 restructure commit: split into AO2.15 (guard lint + evidence gate), AO2.16 (pipeline integrity — declared parallel_safe/early-dispatchable, killing the MAY-clause), AO2.17 (runbook + sc-lint); every AC labeled with its deliverable; normative samples added for the manifest TOML, `contract_hash()` + evidence fragment, and `admission-baseline.json`; runbook/alert have their own ACs; owners named per sprint. |
| 1 | critical-plan-reviewer (sonnet) | `169e8e0ba` | FAIL — 2 Blocking (PR-description waiver token was self-serviceable — unenforceable sign-off; no migration story for pre-hash evidence, which would break all historical rendering), 7 Important (bench risked violating the anti-alternate-pipeline guard; hash canonicalization unspecified; ancestry check not auditable after rebases; ±15% criterion gate on shared runners = flake generator; `on.paths:` vs required-check pending trap; literal-scan over-claimed guard-across-await detection vs clippy's real lints; the same deferred split), 2 minor (rustfmt sentinel survival; unowned D6/D7) | Fixed in round-1 restructure: waiver = quality-mgr-authored PR comment attributed via API `user.login` against a committed approver list; `"pre-contract"` migration state + backfill with no-error rendering; bench contractually calls `CanonicalWriteHandler::commit_write` with the ao4 literal-scan extended to `benches/`; canonical serialization + `hc1-` versioned hash spec with explicit in/out field boundary; `ancestry_checked_against_sha` audit field, immutable; median-of-5, ±25%, per-runner-class baselines, advisory-on-unknown-runner, flake-response protocol; `ci-scope`-style always-register job pattern mandated and `on.paths:` forbidden; guard-across-await moved to clippy deny attributes with literal scan claiming only lexical classes; rustfmt fixture; owners assigned. |
| 2 | plan-scope-reviewer (sonnet) | `4e84b41bf` | **PASS** — all round-1 closures verified across the three new docs; 3 minor (approver-list and host-allowlist TOML samples missing; AO2.17 blanket-parallel_safe vs D4 trigger read as mixed signals) | Minors fixed in round-2 commit: both TOML samples added; AO2.17 dependencies reframed per-deliverable. |
| 2 | critical-plan-reviewer (sonnet) | `4e84b41bf` | FAIL — 1 Blocking (AO2.16's bench binds to the AO2.14-scoped router constructor/commit_write yet claimed parallel_safe while AO2.15 must_follows for mere sentinel placement — contradictory risk assessment), 3 Important (both sprints reimplemented the base-sha diff-scoping idiom; the manifest path was prose-only in AO2.16 — silent-drift risk; module-level clippy denies can't express per-region granularity), 2 minor (backfill re-attempt semantics; waiver API endpoint unnamed) | Fixed in round-2 commit: AO2.16 D3 gains must_follow AO2.14 + AO2.15 (PR-completion; D1–D2 stay immediately dispatchable — intro and Dependencies aligned); shared `scripts/ci/diff_scope.py` helper owned by AO2.15 and consumed by AO2.16 with the literal manifest path quoted verbatim + cross-sprint fixture assertion; clippy denies moved to function-scoped attributes matching sentinel granularity; backfill declared one-time/one-way; issue-comments endpoint named. |
