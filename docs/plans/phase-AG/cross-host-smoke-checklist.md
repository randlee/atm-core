# Phase AG Cross-Host Validation Checklist

## Purpose

Frozen validation matrix for Windows/macOS cross-host interfaces on the `1.3.1`
release line.

## Record Schema

Each row must record:

- `row_id`
- `lane`
- `sender_host`
- `receiver_host`
- `flow`
- `commands_or_entrypoints`
- `expected_result`
- `required_evidence`
- `status`
- `linked_finding`
- `notes`

## Frozen Required Validation Coverage

| Row ID | Lane | Sender Host | Receiver Host | Flow | Commands Or Entrypoints | Expected Result | Required Evidence | Status | Linked Finding | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `AG-VAL-001` | `Lane A` | `Windows` | `Windows` | release-binary doctor on Windows clean-room state | `atm doctor --json` | daemon auto-start succeeds and readiness is healthy or warning-only | command transcript; `doctor --json`; retained log snapshot when daemon-backed | `PENDING` | `—` | Windows clean-room baseline |
| `AG-VAL-002` | `Lane A` | `macOS` | `macOS` | release-binary doctor on macOS clean-room state | `atm doctor --json` | daemon auto-start succeeds and readiness is healthy or warning-only | command transcript; `doctor --json`; retained log snapshot when daemon-backed | `PENDING` | `—` | macOS clean-room baseline |
| `AG-VAL-003A` | `Lane A` | `Windows` | `macOS` | unauthorized-host rejection before mailbox mutation | Windows `atm send --json` from a host not present in the enabled allowlist | remote daemon rejects the peer before mailbox mutation and emits the structured rejection record | sender transcript/JSON; receiver rejection log entry with host/socket/reason; proof no mailbox mutation occurred | `PENDING` | `AG-FIND-004` | required before normal authorized cross-host success rows |
| `AG-VAL-003` | `Lane A` | `Windows` | `macOS` | first live cross-host durable send | Windows `atm send --json` to macOS recipient | daemon-to-daemon channel is live and durable send succeeds | sender JSON result; receiver transcript; retained logs from both hosts | `PENDING` | `—` | first channel bring-up row |
| `AG-VAL-004` | `Lane A` | `macOS` | `Windows` | first reverse-direction durable send | macOS `atm send --json` to Windows recipient | reverse-direction durable send succeeds | sender JSON result; receiver transcript; retained logs from both hosts | `PENDING` | `—` | confirms both directions |
| `AG-VAL-005` | `Lane A` | `Windows` | `macOS` | receiver-side read after `AG-VAL-003` | `atm read --all --json` on macOS | receiver reads the just-delivered message | receiver JSON result; transcript; retained logs when daemon-backed | `PENDING` | `—` | validate read path |
| `AG-VAL-006` | `Lane A` | `macOS` | `Windows` | receiver-side read after `AG-VAL-004` | `atm read --all --json` on Windows | receiver reads the just-delivered message | receiver JSON result; transcript; retained logs when daemon-backed | `PENDING` | `—` | validate reverse read path |
| `AG-VAL-007` | `Lane A` | `macOS` | `Windows` | cross-host ack round-trip | `atm ack ...` after a `--requires-ack` send | original sender sees the reply-state mutation | sender JSON result; receiver JSON result; retained logs from both hosts | `PENDING` | `—` | validate ack mutation |
| `AG-VAL-008` | `Lane A` | `Windows` | `macOS` | degraded notification after durable cross-host send | successful send plus failing notification/hook path | durable send succeeds and degradation is visible | sender JSON result; degraded warning/error evidence; retained logs from both hosts | `PENDING` | `—` | notification degradation must not be misclassified |
| `AG-VAL-009` | `Lane A` | `Windows` | `macOS` | retry-visible interruption and recovery | daemon restart or temporary peer unavailability during Windows -> macOS cross-host flow | retry/recovery remains observable without losing result classification | transcript; retained logs from both hosts; recovery notes | `PENDING` | `—` | bounded recovery proof for the Windows -> macOS direction only |
| `AG-VAL-010` | `Lane B` | `Windows/macOS` | `macOS/Windows` | copied-state revalidation | approved subset rerun on disposable copied state | copied-state lane passes only after Lane A is green | copied-state transcript; sender/receiver JSON; retained logs from both hosts | `PENDING` | `—` | AG.17 defines the exact approved corrective subset when `AG-FIND-005` remains in scope |
| `AG-VAL-011` | `Lane A` | `Windows/macOS` | `Windows/macOS` | transport-security requirement disposition | inspect retained evidence for cross-host transport against documented TCP/TLS requirement and record whether TLS is actually present | AG either captures affirmative TLS evidence or links a named `PRODUCT-BUG` / requirement-drift finding; any release-usable verdict must then explicitly exclude transport-security coverage when TLS is absent | requirement/architecture citation; implementation citation; finding link if TLS is absent; final verdict linkage | `PENDING` | `AG-FIND-001` | mandatory before AG.9 may issue any release-usability claim |
| `AG-VAL-012` | `Lane A` | `Windows` | `Windows` | secure loopback handshake | secure loopback send/receive through the local daemon peer listener | secured transport handshake succeeds locally and ATM payload delivery still works | command transcript; sender/receiver JSON; secure-handshake log evidence | `PENDING` | `—` | secure local proof before second-host reruns |
| `AG-VAL-013` | `Lane A` | `Windows` | `macOS` | secure LAN host-pair rerun | authorized secure daemon-to-daemon send/read/ack on the simplest real network path | secure daemon-to-daemon transport succeeds across the LAN host pair and the AG.7 functional rows still pass | sender/receiver JSON; handshake evidence; retained logs from both hosts | `PENDING` | `AG-FIND-001` | secure rerun of the primary host-pair matrix |
| `AG-VAL-014` | `Lane A` | `Windows` | `macOS` | unauthorized or unauthenticated peer rejected under secured transport | secure daemon-to-daemon attempt with wrong/missing trust material | remote daemon rejects the peer before mailbox mutation under the secured transport path | sender transcript/JSON; receiver rejection log entry; proof no mailbox mutation occurred | `PENDING` | `AG-FIND-001` | secure-mode rejection proof |
| `AG-VAL-015` | `Lane A` | `Windows` | `macOS` | secure routed/VPN host-pair rerun | authorized secure daemon-to-daemon send/read/ack across the routed/VPN path | secure daemon-to-daemon transport succeeds across the routed/VPN host pair and preserves the AG.7 functional guarantees | sender/receiver JSON; handshake evidence; retained logs from both hosts | `PENDING` | `AG-FIND-001` | secure rerun of the secondary routed/VPN path |
| `AG-VAL-016` | `Lane A` | `Same Host` | `Same Host` | localhost remote-target rejection path | `atm send --json <agent>@<team>.localhost ...` or `atm send --json <agent>@<team> --host localhost ...` from a host not present in the enabled allowlist | remote-target parsing selects the peer-transport path and unauthorized localhost traffic is rejected before mailbox mutation | sender JSON/transcript; daemon log showing remote-target dispatch and rejection; proof no local mailbox write occurred | `PENDING` | `AG-FIND-005` | first corrective proof that remote-target sends no longer fall back to the local mailbox path |
| `AG-VAL-017` | `Lane A` | `Same Host` | `Same Host` | localhost full-function remote-target success | authorized localhost remote-target send/read/ack through the daemon peer listener using real ATM payloads | the full functional matrix succeeds on localhost: durable send, receiver read, `--requires-ack`, reply-state mutation, nudge/notification classification, and retry-visible recovery | sender/receiver JSON; daemon logs; retained notification evidence; retry/recovery transcript | `PENDING` | `AG-FIND-005` | localhost is the lowest-friction full-function proof before public-interface or second-host smoke |
| `AG-VAL-018` | `Lane A` | `Same Host` | `Same Host` | localhost transport-security disposition | run the active localhost remote-target matrix under the current release candidate's transport mode and record whether the localhost proof is unsecured or secured | the localhost proof records whether transport security is absent or present and links the result to `AG-FIND-001` when TLS is still open | command transcript; implementation citation; daemon logs; finding link when insecure | `PENDING` | `AG-FIND-001` | corrective localhost evidence must not silently imply encryption closure |
| `AG-VAL-019` | `Lane A` | `Same Host` | `Same Host` | public-interface loopback rejection path | remote-target send to the host's non-loopback advertised address from a non-allowlisted identity | public-interface addressing selects the peer-transport path and unauthorized traffic is rejected before mailbox mutation | sender JSON/transcript; daemon log showing remote-target dispatch and rejection; proof no local mailbox write occurred | `PENDING` | `AG-FIND-005` | proves the same dispatch branch works off-loopback on one host |
| `AG-VAL-020` | `Lane A` | `Same Host` | `Same Host` | public-interface loopback full-function success | authorized remote-target send/read/ack through the host's non-loopback advertised address using real ATM payloads | the full functional matrix succeeds over the public-interface path: durable send, receiver read, `--requires-ack`, reply-state mutation, nudge/notification classification, and retry-visible recovery | sender/receiver JSON; daemon logs; retained notification evidence; retry/recovery transcript | `PENDING` | `AG-FIND-005` | final same-host proof before second-host smoke |
| `AG-VAL-021A` | `Lane A` | `macOS` | `macOS` | other-Mac unauthorized rejection | non-allowlisted remote-target send against another Mac on the same candidate line | the receiving Mac rejects the peer before mailbox mutation | sender/receiver evidence; daemon logs from both Macs; proof no mailbox mutation occurred | `PENDING` | `AG-FIND-005` | use the simplest reachable Mac-to-Mac path first |
| `AG-VAL-021B` | `Lane A` | `macOS` | `macOS` | other-Mac authorized durable send | authorized remote-target send against another Mac on the same candidate line | durable send succeeds across the Mac-to-Mac host pair | sender JSON; receiver transcript; daemon logs from both Macs | `PENDING` | `AG-FIND-005` | second-host send success |
| `AG-VAL-021C` | `Lane A` | `macOS` | `macOS` | other-Mac receiver read | `atm read --all --json` on the receiving Mac after `AG-VAL-021B` | receiver reads the just-delivered message | receiver JSON/transcript; daemon logs from both Macs | `PENDING` | `AG-FIND-005` | second-host read success |
| `AG-VAL-021D` | `Lane A` | `macOS` | `macOS` | other-Mac ack round-trip | `atm ack ...` after a `--requires-ack` send on the Mac-to-Mac pair | original sender sees the reply-state mutation | sender/receiver JSON; daemon logs from both Macs | `PENDING` | `AG-FIND-005` | second-host ack success |
| `AG-VAL-021E` | `Lane A` | `macOS` | `macOS` | other-Mac nudge/notification classification | successful durable send plus failing notification/hook path on the Mac-to-Mac pair | durable send succeeds and notification degradation remains visible without being misclassified as delivery failure | sender JSON; notification evidence; daemon logs from both Macs | `PENDING` | `AG-FIND-005` | second-host notification behavior |
| `AG-VAL-021F` | `Lane A` | `macOS` | `macOS` | other-Mac retry-visible recovery | daemon restart or temporary peer unavailability during the Mac-to-Mac flow | retry/recovery remains observable without losing result classification | transcript; daemon logs from both Macs; recovery notes | `PENDING` | `AG-FIND-005` | second-host recovery behavior |
| `AG-VAL-022A` | `Lane A` | `Windows` | `macOS` | Windows/macOS unauthorized rejection | non-allowlisted remote-target send against the Windows/macOS host pair | the receiving daemon rejects the peer before mailbox mutation | sender/receiver evidence; daemon logs from both hosts; proof no mailbox mutation occurred | `PENDING` | `AG-FIND-005` | final heterogeneous-host rejection proof |
| `AG-VAL-022B` | `Lane A` | `Windows` | `macOS` | Windows/macOS authorized durable send | authorized remote-target send against the Windows/macOS host pair | durable send succeeds across the heterogeneous-host pair | sender JSON; receiver transcript; daemon logs from both hosts | `PENDING` | `AG-FIND-005` | heterogeneous-host send success |
| `AG-VAL-022C` | `Lane A` | `Windows` | `macOS` | Windows/macOS receiver read | `atm read --all --json` on the receiving host after `AG-VAL-022B` | receiver reads the just-delivered message | receiver JSON/transcript; daemon logs from both hosts | `PENDING` | `AG-FIND-005` | heterogeneous-host read success |
| `AG-VAL-022D` | `Lane A` | `Windows` | `macOS` | Windows/macOS ack round-trip | `atm ack ...` after a `--requires-ack` send on the Windows/macOS pair | original sender sees the reply-state mutation | sender/receiver JSON; daemon logs from both hosts | `PENDING` | `AG-FIND-005` | heterogeneous-host ack success |
| `AG-VAL-022E` | `Lane A` | `Windows` | `macOS` | Windows/macOS nudge/notification classification | successful durable send plus failing notification/hook path on the Windows/macOS pair | durable send succeeds and notification degradation remains visible without being misclassified as delivery failure | sender JSON; notification evidence; daemon logs from both hosts | `PENDING` | `AG-FIND-005` | heterogeneous-host notification behavior |
| `AG-VAL-022F` | `Lane A` | `Windows` | `macOS` | Windows/macOS retry-visible recovery | daemon restart or temporary peer unavailability during the Windows/macOS flow | retry/recovery remains observable without losing result classification | transcript; daemon logs from both hosts; recovery notes | `PENDING` | `AG-FIND-005` | final heterogeneous-host recovery proof |
